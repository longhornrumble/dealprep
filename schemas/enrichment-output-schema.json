{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://myrecruiter.ai/schemas/deal-prep/enrichment-output-schema.json",
  "title": "Person Enrichment Output Schema",
  "description": "Canonical person enrichment output contract (Implementation Spec §7.3)",
  "type": "object",
  "required": ["requester_profile", "errors"],
  "properties": {
    "requester_profile": {
      "type": "object",
      "description": "Enriched profile information for the requester",
      "required": ["summary", "confidence"],
      "properties": {
        "summary": {
          "type": "string",
          "description": "Summary of the requester's profile. Must be 'Not found' if unavailable."
        },
        "confidence": {
          "type": "string",
          "enum": ["high", "medium", "low", "not_available"],
          "description": "Confidence level of the enrichment data. Must be 'not_available' when summary is 'Not found'."
        }
      }
    },
    "errors": {
      "type": "array",
      "description": "Errors encountered during enrichment",
      "items": {
        "type": "string"
      },
      "default": []
    }
  }
}
